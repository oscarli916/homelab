apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: opencode
  namespace: opencode
  annotations:
    cert-manager.io/cluster-issuer: home-arpa-ca
    gethomepage.dev/enabled: "true"
    gethomepage.dev/name: OpenCode
    gethomepage.dev/description: Development tool
    gethomepage.dev/group: Apps
    gethomepage.dev/icon: code
    gethomepage.dev/href: https://opencode.home.arpa
spec:
  ingressClassName: traefik
  rules:
    - host: opencode.home.arpa
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: opencode-web
                port:
                  number: 3000
  tls:
    - hosts:
        - opencode.home.arpa
      secretName: opencode-home-arpa-tls
