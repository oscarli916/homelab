apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: infrastructure-tailscale
  namespace: flux-system
spec:
  dependsOn:
    - name: infrastructure-sealed-secrets
  interval: 10m0s
  path: ./clusters/homelab/infrastructure/tailscale
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
