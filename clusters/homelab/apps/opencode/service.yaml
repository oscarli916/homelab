apiVersion: v1
kind: Service
metadata:
  name: opencode-web
  namespace: opencode
  labels:
    app.kubernetes.io/name: opencode
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: opencode
  ports:
    - name: http
      port: 3000
      targetPort: http
      protocol: TCP
