apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: infrastructure-kube-prometheus-stack-config
  namespace: flux-system
spec:
  dependsOn:
    - name: infrastructure-kube-prometheus-stack
  interval: 10m0s
  path: ./clusters/homelab/infrastructure/kube-prometheus-stack-config
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
