apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: infrastructure-kube-prometheus-stack
  namespace: flux-system
spec:
  dependsOn:
    - name: infrastructure-cert-manager-issuers
    - name: infrastructure-longhorn
    - name: infrastructure-tailscale
  interval: 10m0s
  path: ./clusters/homelab/infrastructure/kube-prometheus-stack
  prune: true
  wait: true
  healthChecks:
    - apiVersion: helm.toolkit.fluxcd.io/v2
      kind: HelmRelease
      name: kube-prometheus-stack
      namespace: flux-system
  sourceRef:
    kind: GitRepository
    name: flux-system
